# PenguinCSS

A micro-CSS framework which provides components in the style of the Club Penguin UI.

The goal of PenguinCSS isn't to make a fully fledged CSS framework with utility/flex/responsive classes. It's just to provide some basic styles which to let you recreate some of the classic Club Penguin UI elements.
